type HeroType = {
    response: string,
    id: string,
    name: string,
    powerstats: object,
    biography: object,
    appearance: object
    work: object,
    connections: object,
    image: {
      url: string
    }
}

export default HeroType;